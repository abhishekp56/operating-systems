#include <stdio.h>

int main() {
    int bt[10], n, i, j, t;
    scanf("%d",&n);
    for(i=0;i<n;i++) scanf("%d",&bt[i]);
    for(i=0;i<n;i++)
        for(j=i+1;j<n;j++)
            if(bt[i]>bt[j]) { t=bt[i]; bt[i]=bt[j]; bt[j]=t; }
    for(i=0;i<n;i++) printf("%d ",bt[i]);
    return 0;
}
